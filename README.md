# FAP'Inc — Character Sheets

Player character sheets for Marcel's *Rime of the Frostmaiden* campaign.
Live at **https://liquidmasl.github.io/rime-sheets/**

Bookmark your own character directly:
`#veil` · `#schneehaar` · `#fuenfin` · `#fjern` · `#rinaldo`

There's a **print** button in the header if you want paper.

---

⚠️ **Generated — do not edit `index.html` here.**
Source lives in the private campaign repo at `sheets/build.py`; rebuild with:

```bash
uv run --directory ~/repos/dm-toolbox python \
    ~/repos/rime-of-the-frostmaiden/sheets/build.py ~/repos/rime-sheets
```

The build runs a spoiler audit before writing, so DM-only terms can't reach this
public repo by accident.
